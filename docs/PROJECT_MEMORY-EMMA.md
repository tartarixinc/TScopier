# Project Memory - Emma (emmydapson / Ilori Emmanuel)

Changelog entries authored by Emma, kept separate from the main PROJECT_MEMORY.md by request.

## Changelog

### 2026-09-03 - Stefan Production Readiness After Staging Acceptance

- **Original failure:** Stefan's second complete same-channel XAUUSD BUY setup refreshed the first basket's SL/TP instead of opening an independent basket.
- **Entry root cause:** Real Telegram persistence could keep the explicit entry zone only in `parsed_data._intent.entry`; the merge guard originally checked top-level entry fields, so the complete second setup looked like a parameter refresh. Automated fixtures missed this shape until real staging retest exposed it.
- **Entry fix and staging PASS:** Complete unlinked same-side signals with their own entry/zone, SL, and TP now bypass basket refresh and open independently. Staging confirmed Signal A opened Basket A and Signal B opened Basket B.
- **Management regression:** Staging then exposed that a reply-linked `Move SL to breakeven` to Signal A affected both same-symbol baskets because `applyManagement(...)` used channel-wide trade loading even when a Telegram parent identity existed.
- **Management fix and staging PASS:** Explicit reply/parent management now resolves the replied parent to a basket anchor and scopes management to trades for that `signal_id`; unthreaded management fallback is unchanged. Staging confirmed Basket A breakeven was applied and later hit, Basket B stayed open with original SL/TP, and no new basket was created.
- **Production readiness:** Temporary Stefan debug instrumentation was removed after acceptance. Ready for production PR after final focused validation; no push, merge, deploy, or full-suite claim was made here.
### 2026-09-03 - Stefan Reply-Linked Management Basket Scoping

- **Stefan independent-entry staging PASS:** Staging confirmed the original Stefan multi-signal fix: Signal A opened its own XAUUSD BUY basket, and a second complete XAUUSD BUY opened as independent Basket B instead of refreshing Basket A.
- **Reply-linked management regression:** A Telegram reply to Signal A saying `Move SL to breakeven` affected positions from both Basket A and Basket B. That regression is separate from the independent-entry fix and only concerns explicit reply-linked management scoping.
- **Root cause:** `applyManagement(...)` detected reply-scoped management but still loaded open trades through the channel-wide management loaders. With two same-channel XAUUSD BUY baskets open, breakeven eligibility included both baskets, so `OrderModify` moved SL on both baskets; any later broker closure would be caused by those SL modifications, not by a new entry merge.
- **Corrected fix:** Explicit Telegram reply/parent management now resolves the replied parent to an open basket anchor and loads trades by that `signal_id` across brokers. Unthreaded management keeps the existing channel/symbol fallback. Reply-scoped close cleanup and channel-active-params persistence no longer widen an explicit target back to the whole channel.
- **Regression coverage:** Added an executor-level test with Basket A and Basket B open, then a breakeven reply to Signal A. The test asserts only Basket A tickets are modified, Basket B is untouched, no new basket is created, reply to Signal B targets Basket B, and existing single-basket management still works.
- **Staging retest pending:** No push, deploy, or live broker retest was performed after this local fix. Staging still needs a manual Telegram reply retest confirming reply-to-A manages Basket A only and Basket B remains untouched.

### 2026-09-02 - Stefan Live Multi-Signal Basket Refresh Correction

- **Previous fixture gap:** The earlier Stefan regression passed with the deterministic parser shape for the second complete GOLD BUY setup. That fixture had top-level `entry_zone_low` / `entry_zone_high`, so `shouldRouteAsBasketParameterRefresh(...)` rejected the message before the modify-only basket refresh path could reproduce the live failure.
- **Real Telegram acceptance failure:** The real Signal 2 acceptance still reached modify-only basket refresh and updated the existing XAUUSD BUY leg instead of opening a new trade. The failure-shaped row had action/SL/TP data that looked like a same-channel same-symbol same-direction parameter refresh because the top-level entry anchor fields were absent by the time merge routing evaluated it.
- **Actual root cause:** The unlinked complete-entry guard recognized legacy top-level `entry_price` / `entry_zone_*` and now raw `Gold Buy Zone ...` text, but the live universal/persisted shape can retain the entry anchor as stored intent data at `parsed_data._intent.entry`. Without reading that path, a complete unthreaded setup with its own entry zone could still look like an SL/TP refresh candidate.
- **Corrected fix:** `mergeRouting.ts` now treats stored universal intent `parsed_data._intent.kind === 'entry'` with a positive `entry` array as an explicit entry anchor for the unlinked complete-entry merge guard. It also respects `_intent.flags.market_now` so teaser-completion/market-now flows remain eligible for their existing merge behavior.
- **Management safety:** The guard still requires explicit SL/TP, an entry anchor, no market-now intent, and no reply/thread/parent/same-signal revision link. SL-only updates, breakeven, partial close, close, explicit reply/thread updates, same-message edits, and market-now teaser completion are not weakened by this fix.
- **Verification:** Focused local tests passed for Stefan local behavior, merge routing, and multi-trade merge. Worker build passed. Changed-file ESLint passed. `git diff --check` passed.
- **Manual retest pending:** No commit, push, deploy, Supabase function change, or live broker/Telegram retest was performed. Stefan's real Telegram two-signal flow still needs manual retest on the live/staging path.

### 2026-09-02 - Minimal Assistant Human Handoff For Unresolved Product Issues

- **Stefan fix reconfirmed:** The Stefan same-side GOLD Signal 3 / Signal 4 worker behavior remains unchanged from the prior fix. Local focused worker verification still shows Signal 3 preserved, Signal 4 kept as a separate signal, Signal 4 reaching new BUY entry intent, no same-basket SL/TP refresh, and genuine management messages still parsing as management.
- **No product blocker:** Stefan's issue was code behavior, not a customer setting. The assistant now states that disabling `add_new_trades_to_existing` is not the solution and that complete unlinked second entries should now open independently; if the user is still affected, it escalates instead of guessing.
- **Minimal escalation added:** Added the narrow `escalate_support_issue` assistant tool for unresolved product bugs, low-confidence/evidence-unavailable diagnostics, and account-blocking issues such as Telegram login-code delivery after bounded safe checks. Categories are limited to `telegram_auth`, `copier_execution`, `account_access`, `configuration`, `product_bug`, and `unknown`; severity is `normal` or `high`.
- **Persistence and dedupe:** No general support-case table or immediate internal notifier existed beyond trade-specific `trade_reports`, contact/live chat, and notification UI. Added backend-only `assistant_support_escalations` persistence with a per-user fingerprint dedupe window of one hour so repeated equivalent handoffs reuse the existing row.
- **Notification:** No Slack/Telegram/email ops notifier or admin notification queue was introduced. The handoff is safely persisted for support review; immediate team notification remains unavailable until an existing internal path is added or selected.
- **Security boundaries:** The model cannot choose trusted user IDs or recipients. The backend scopes writes to the authenticated user, verifies broker/channel/signal IDs before storing them as trusted context, strips passwords, broker credentials, API keys, tokens, Telegram session strings, login codes, unverified identity fields, raw payload/body/header fields, and raw latest-user-message excerpts.
- **Local acceptance:** `npx.cmd vitest run src/lib/assistantProductSupport.test.ts` passed 9/0, including Stefan product-specific answer, unresolved Stefan escalation summary, Telegram login-code handoff, sanitized record shape, and duplicate suppression. Existing Node assistant tests passed 30/0. The Stefan worker focused suite passed 143/0. Root build and worker build passed; changed-file ESLint passed; `git diff --check` and conflict-marker scan passed. Deno remains unavailable locally, so Edge shared tests were added but not live-executed with `deno test`.
- **Staging pending:** No commit, push, deploy, migration apply, live LLM call, or broker order was performed. Staging still needs live assistant and safe broker-boundary verification.

### 2026-09-01 - Stefan Same-Side GOLD Signal Merge Investigation

- **Customer issue:** Stefan reported that his Telegram channel can post multiple independent GOLD BUY setups while prior GOLD BUY setups remain active. His second complete setup (`GOLD BUY SETUP`, `Gold Buy Zone 4441 - 4436`, `SL : 4531`, `TP1/TP2/TP3`, `TP4 : Hold`) appeared to modify/manage the existing GOLD BUY trade from the earlier complete setup instead of opening its own independent signal/trade set.
- **Root cause:** This was code behavior, not a usable customer setting. `worker/src/tradeExecutor/basketMerge/mergeRouting.ts` called `tryMergeSignalIntoExistingOpenTrade(...)` when `manual_settings.add_new_trades_to_existing=true`. The merge-link context from `worker/src/signalMergeLink.ts` could mark any same-channel same-symbol/same-direction SL/TP-bearing message inside the long same-channel window as `parameterRefreshSameChannel`, so `link.isLinked` became true even when the new Telegram post was a complete fresh entry with its own entry zone. That routed Signal 4 to `applyBasketSlTpRefresh(...)` against the newest prior basket and could mutate Signal 3's open legs.
- **Configuration finding:** The existing field is `manual_settings.add_new_trades_to_existing`. It defaults to true in account/channel manual settings. Setting it to false is not Stefan's intended solution because the entry path then behaves as a single-slot mode and skips new same-symbol entries while an open same-symbol trade exists. Per-channel effective config is resolved through `broker_channel_trading_configs`; table-backed channel config takes precedence over stale broker JSON, and omitted manual fields normalize to defaults.
- **Behavior before:** Two unthreaded complete same-channel GOLD BUY posts within the merge window could be treated as one basket refresh solely because the newest open basket shared symbol, direction, channel, and time window.
- **Behavior after:** An unlinked complete entry with explicit entry anchor/zone plus explicit SL/TP, and no market-now teaser-completion wording, is no longer eligible for `tryMergeSignalIntoExistingOpenTrade(...)`. It falls through to normal entry dispatch so the second setup receives its own signal identity and order-send path. Explicit replies/thread links, same-signal Telegram edits, SL/TP-only parameter follow-ups, breakeven, partial-close, close, and teaser-completion flows remain eligible for management/merge routing.
- **Identity limitation:** The architecture can correlate management by Telegram message id, signal id, parent/reply relationship, channel id, symbol/direction, and basket anchor. When a provider later sends an unthreaded symbol-only management message such as "Move SL to breakeven" while multiple same-channel same-symbol same-direction baskets are active, there may still be no reliable provider identity telling TScopier which basket was intended. Current management routing must use the existing anchor/scope rules in that ambiguous case; this fix does not invent identity that Telegram/provider text did not provide.
- **Tests added:** Added Stefan's two complete GOLD BUY parsing regression, same-side complete-entry merge guard, genuine breakeven management guard, close/partial management guard, same-symbol opposite-direction full-entry guard, per-channel config override/default proof, and duplicate Telegram replay protection.
- **Verification:** Focused worker regression run passed: `node --require ts-node/register --test src/parseSignal.test.ts src/multiTradeMerge.test.ts src/tradeExecutor/basketMerge/mergeRouting.test.ts src/channelTradingConfig.test.ts src/ensureSignalRow.test.ts` with 140 pass / 0 fail. Staging still needs a safe broker-boundary reproduction confirming Signal 3 remains active, Signal 4 creates its own execution intent, and real management messages still manage the intended trade.
- **Post-dev-sync local verification:** After reconciling local `dev` with `origin/dev` and the new Sentry staging-test commits, reran the expanded Stefan-focused suite including `worker/src/tradeExecutor/stefanLocalBehavior.test.ts`: 143 pass / 0 fail. The local behavior seam parsed Signal 3 and Signal 4 as complete GOLD BUY entries, persisted distinct signal identities, seeded Signal 3 as an active basket, and observed `tryMergeSignalIntoExistingOpenTrade(...)` return `handled=false` for Signal 4 with log `merge skipped for unlinked complete entry`; Signal 4 then reached a new BUY execution intent at the broker boundary without refreshing Signal 3.
- **Management verification:** The local behavior test also confirmed genuine management text still stays management-classified: `Move SL to breakeven` parses as breakeven, `TP1 hit, close half and move SL to entry` parses as partial breakeven/partial-close equivalent, `Close trade now` parses as close, and explicit reply-linked full entries remain merge-eligible. The fix therefore guards only unlinked complete new entries, not legitimate management/update flows.
- **AI support failure exposed by Stefan:** The customer-facing assistant could previously answer with generic copier/trading guidance because its prompt only had broad range/multi-trade notes and `get_channel_config` did not expose effective configuration precedence, `add_new_trades_to_existing` semantics, or product facts showing that no customer setting solved Stefan's overlapping complete-signal case.
- **AI grounding architecture:** Added a shared Deno-friendly product-support knowledge module, injected product-support context into `assistant-chat` based on the user's latest question, added a controlled `get_product_support_context` tool, and enriched `get_channel_config` with `effective_manual_settings`, `config_source`, `stored_channel_configured`, linked-channel count, config precedence, and customer-safe support notes. The floating assistant UI was not redesigned.
- **Assistant product/config context:** The assistant can now ground product-specific answers in curated TScopier facts, `DEFAULT_MANUAL_SETTINGS` / normalized manual settings, channel config resolution semantics (`defaults -> broker/account -> channel override`, with missing assigned multi-channel configs healing from defaults and missing assigned single-channel configs healing from broker manual settings), `broker_channel_trading_configs`, effective range-trading and lot-size fields, and existing safe diagnostic tools for recent trades, copier logs, and trade detail.
- **Assistant safety/privacy boundaries:** The assistant still has no raw SQL or unrestricted database access. Tool responses are scoped to the authenticated user and sanitized. The new context must not expose broker passwords, API keys, access tokens, Telegram sessions, Supabase service secrets, credentials, raw sensitive Telegram/broker payloads, or unrelated users' data.
- **AI tests/evals added:** Added deterministic semantic acceptance coverage for Stefan's exact support question, range-trading channel setting questions, trade-copy diagnostic questions, lot-size effective-config questions, and unsupported/unknown product questions that should admit uncertainty instead of inventing settings. Local Vitest validation passed 5 pass / 0 fail. A matching Supabase/Deno test file was added, but local Deno execution is blocked because `deno` is not installed on this machine.
- **Remaining limitations:** Local verification stopped at the existing broker/execution boundary; no live broker order was sent. The Edge Function/OpenAI chat path was validated through deterministic context/model-input construction and frontend build/lint, not through a live local Deno function call, because Deno and external LLM execution are unavailable locally. Staging must still verify the live assistant response with a real authenticated account/channel context and a safe broker-boundary Stefan reproduction.

### 2026-08-25 - Critical Sentry Runtime Health For FxSocket And Worker Liveness

- **Context:** A sustained FxSocket/API disconnect was not previously surfaced as a critical Sentry incident. The worker had substantial Sentry coverage for fatal exceptions and user-impacting business events, but the FxSocket WebSocket reconnect path could keep retrying with only local logs and no engineering alert when market-data/socket infrastructure stayed unavailable.
- **Responsibility boundary:** Sentry critical health now answers "is TScopier runtime or infrastructure broken?" Admin/logging remains the source of truth for "why did this user's signal, trade, or account behave this way?" Individual rejected trades, malformed signals, user/account configuration failures, normal broker business rejections, and recoverable trade-level outcomes intentionally remain admin/business diagnostics unless they indicate a systemic outage.
- **Architecture:** Added `worker/src/observability/criticalHealth.ts` as a health state/event model, not a second Sentry transport layer. It defines bounded component/failure metadata, process-local cooldown suppression, resource-id hashing, `captureCriticalHealthIssue(...)`, and `SustainedOutageTracker`. It delegates actual Sentry transport to the existing `worker/src/observability/sentry.ts` helper, preserving existing redaction, scope, tags, fingerprints, and fire-and-forget behavior.
- **FxSocket behavior:** `worker/src/fxsocketWsClient.ts` now wires WebSocket lifecycle events into a sustained-outage tracker. A short disconnect inside `FXSOCKET_SOCKET_OUTAGE_GRACE_MS` stays log-only. If the current socket remains unavailable beyond the grace period, the worker emits one critical-health event with `component=fx_socket`, `failure_class=sustained_outage`, `provider=fxsocket`, and reason code `FXSOCKET_SOCKET_SUSTAINED_OUTAGE`. Reconnect attempts continue normally and update metadata; successful reconnect resets outage state so a later legitimate outage can alert again.
- **Race hardening:** Every socket-bound handler for the captured `const ws = ...` now checks active identity before mutating connection or health state: stale `open`, `message`, `close`, and `error` events are ignored when `this.ws !== ws`. This prevents an old socket's late close from scheduling reconnect or starting a false outage timer after a replacement socket is already healthy.
- **Timer invariant:** `SustainedOutageTracker.scheduleAlert()` enforces one active grace timer per active outage. First disconnect schedules the timer; repeated reconnect attempts only update metadata and do not allocate additional timers. Recovery clears the timer. After the timer fires, state handling remains valid; after recovery, a future outage gets a fresh timer.
- **Dedupe/grouping:** FxSocket sustained-outage cooldown is provider scoped inside one worker process: `fx_socket|fxsocket|<platform>|<endpoint_host>`. It excludes account/user identity and excludes API keys or query parameters. Multiple FxSocket clients in one worker share process-local suppression, but equivalent events from separate worker processes can still reach Sentry. The shared fingerprint `critical_health / fx_socket / sustained_outage / fxsocket` groups equivalent events into the same Sentry issue across workers.
- **Worker heartbeat:** Added guarded `captureWorkerCheckIn(...)` in the existing Sentry helper and `worker/src/observability/workerHeartbeat.ts` for opt-in Sentry monitor check-ins. `worker/src/index.ts` starts heartbeat check-ins only when `SENTRY_WORKER_HEARTBEAT_MONITOR_SLUG` is configured and stops the interval during shutdown. This detects missed check-ins/process liveness only; it does not prove the trade/copier pipeline is actively processing work. `/health`, queue metrics, copier health rows, and admin diagnostics remain necessary for readiness and workflow-level health.
- **Runtime isolation:** Monitoring is isolated from WebSocket runtime behavior. `FxsocketWsClient` calls monitor methods through a local `reportHealth(...)` wrapper, and Sentry helpers catch their own failures. A disabled/missing/throwing Sentry SDK path cannot break socket open/message/close handling, reconnect scheduling, queue processing, or trading behavior. Invalid JSON frames no longer update last-success activity metadata.
- **Security/privacy:** Critical-health metadata is bounded and sanitized. It can include component, failure class, severity, state, environment, provider, platform, endpoint host, reconnect attempt/delay, duration, and hashed account/resource identifiers. It must not include FxSocket API keys, auth tokens, credentials, raw Telegram messages, raw signal payloads, broker payloads, account balances/equity/free margin, request/response bodies, or full endpoint URLs with query strings.
- **Env vars:** Added `SENTRY_CRITICAL_HEALTH_ENABLED=true`, `SENTRY_CRITICAL_HEALTH_COOLDOWN_MS=300000`, `FXSOCKET_SOCKET_OUTAGE_GRACE_MS=60000`, `SENTRY_WORKER_HEARTBEAT_MONITOR_SLUG=`, `SENTRY_WORKER_HEARTBEAT_ENABLED=true`, `SENTRY_WORKER_HEARTBEAT_INTERVAL_MS=60000`, and `SENTRY_WORKER_HEARTBEAT_CHECKIN_MARGIN_MINUTES=2` to `worker/.env.example`. Sentry itself remains globally disabled unless `SENTRY_ENABLED=true` and `SENTRY_DSN` are set.
- **Tests/validation:** Added `worker/src/observability/criticalHealth.test.ts`, `worker/src/observability/workerHeartbeat.test.ts`, and `worker/src/fxsocketWsClient.health.test.ts`. Coverage includes short disconnect no-alert, sustained outage one alert, reconnect retry dedupe, single active grace timer, recovery reset, second later outage, stale old socket close/open/message/error guards, provider-scoped two-client dedupe, throwing monitor isolation, duplicate close events, Sentry disabled behavior, heartbeat slug gating/config/failure isolation, and existing FxSocket stream manager behavior. Validation passed: focused critical-health/FxSocket/heartbeat test set 21 pass / 0 fail, worker build, changed-file ESLint, `git diff --check`, and conflict-marker scan.
- **Upstream reconciliation:** The work was preserved in a named Git stash plus an exported patch, local `dev` was fast-forwarded from `454b1c1d` to `fe0bd785`, and the critical-health changes were reapplied cleanly with no conflicts. Upstream realtime retry hardening in `worker/src/sessionManager.ts` and `worker/src/sessionManager.realtime.test.ts`, plus upstream Sentry fatal-log/redaction behavior, were preserved. No commit, push, deploy, rebase, merge commit, or force reset was performed.
- **Staging acceptance:** Configure Sentry on staging worker services, set a stable non-secret heartbeat slug per worker service/role, verify a short FxSocket disconnect reconnects without a critical issue, verify a sustained FxSocket outage emits one grouped `fx_socket/sustained_outage` issue after the configured grace, verify multiple active clients in one worker do not create an alert storm, verify reconnect recovery resets state, verify missed heartbeat/check-in alerting reflects process death or hard stall only, and confirm no sensitive values appear in Sentry events/logs.
- **Remaining gaps:** pg_cron jobs still need their own check-in or external monitor because a worker cannot prove a database cron never ran. Broker/RPC systemic outage aggregation is still not implemented; current broker failures are mostly per-account/per-request diagnostics and should not become critical Sentry noise until there is a reliable systemic signal.

### 2026-08-20 - Management breakeven failure diagnostics

- **Context:** Admin diagnostics had a large "Management breakeven / Detailed reason unavailable" bucket. Investigation showed the worker already persisted useful per-leg broker errors for `mgmt_breakeven` and `mgmt_partial_breakeven`, but aggregate breakeven partial failures, reconcile jobs, and sweep exhaustion mostly kept counts or generic text.
- **Root cause:** `worker/src/tradeExecutor/managementExecutor.ts` preserved raw per-leg `error_message`, but did not also persist a safe normalized reason code/structured failure payload. `BREAKEVEN_PARTIAL` business events and `basket_reconcile_jobs.last_error` did not carry distinct underlying cause categories, and `mgmt_sweep_max_redispatch` described retry exhaustion without preserving the original breakeven failure cause.
- **Solution:** Added `worker/src/managementBreakevenDiagnostics.ts` to normalize management-breakeven failures using existing broker/modify classifiers, persist safe per-leg metadata in `trade_execution_logs.request_payload`, aggregate bounded underlying reason codes for `BREAKEVEN_PARTIAL`, append safe reason codes to reconcile `last_error`, and log sweep exhaustion separately from preserved underlying cause. Breakeven target math, BUY/SELL direction math, broker modify calls, verification, reconcile queuing, retry behavior, and later SL precedence were not changed.
- **Affected files:** `worker/src/managementBreakevenDiagnostics.ts`, `worker/src/managementBreakevenDiagnostics.test.ts`, `worker/src/tradeExecutor/managementExecutor.ts`, `worker/src/tradeExecutor/TradeExecutor.ts`, `docs/PROJECT_MEMORY-EMMA.md`.
- **Verification:** Passed `node --import tsx --test worker/src/managementBreakevenDiagnostics.test.ts`; passed targeted suite `node --import tsx --test worker/src/managementBreakevenDiagnostics.test.ts worker/src/autoManagement.test.ts worker/src/basketEffectiveStops.test.ts worker/src/basketSlTpReconcile.test.ts worker/src/basketLegSync.test.ts worker/src/observability/businessEvents.test.ts` (73 tests); passed `node --import tsx --test worker/src/managementPendingLegs.test.ts worker/src/basketModFollowUp.test.ts`; passed targeted ESLint for changed worker files; passed worker no-emit typecheck; passed worker build. `npm --prefix worker run build` was blocked by PowerShell script policy, rerun as `npm.cmd --prefix worker run build`. Worker no-emit/build required elevated filesystem access because sandboxed Node could not `lstat C:\Users\user`.
- **Blockers:** No DB migration was added. `basket_reconcile_jobs` has no existing JSON diagnostics column in the audited type, so normalized reconcile detail is stored safely in existing text `last_error` instead of a structured job metadata object.
- **Follow-up:** Staging should confirm Admin reads `trade_execution_logs.request_payload.reason_code`, `trade_failure`, aggregate `underlying_reason_codes`, and the sweep exhaustion log before treating the old "Detailed reason unavailable" bucket as closed. No commit, push, deploy, migration, broker connection, or trade execution was performed.

### 2026-08-13 - Telegram Login-Code Resend/Fallback Incident

- **Incident:** Production evidence showed `auth.SendCode` was definitely invoked and Telegram returned success with delivery `app`, but at least three users did not see a Telegram login code in their Telegram app. Repeating the request created a fresh `sendCode` request and fresh `phoneCodeHash`, and pending auth eventually expired. A malformed earlier phone input correctly surfaced `PHONE_NUMBER_INVALID`.
- **Root architectural gap:** The phone auth flow treated every retry as a new `auth.SendCode` attempt. It retained only `phoneCodeHash` plus broad delivery and discarded Telegram `auth.SentCode.next_type`, `timeout`, and code length metadata. There was no `auth.resendCode` implementation, so the UI could not follow Telegram's documented "wait timeout, then resend with the existing hash" fallback path.
- **Fix implemented:** Added a minimal pending phone auth state model that keeps the current server-only `phoneCodeHash`, current delivery, `next_type` mapped to safe UI delivery metadata, timeout-derived `resendAvailableAt`, and code length. The metadata is stored inside the existing short-lived `telegram_auth_pending.auth_session_string` envelope for phone auth rows, avoiding migrations and keeping QR auth rows compatible.
- **Resend behavior:** Added worker `/auth/resend_code`, Edge action `resend_code`, shared frontend typing, and dashboard/onboarding resend actions. Before Telegram's timeout expires, resend is rejected locally with the remaining wait. After timeout, the worker calls `Api.auth.ResendCode` using the existing phone number and current server-side hash, consumes the returned `auth.SentCode`, and replaces the old hash with the returned hash as the single authoritative hash.
- **UX behavior:** The code screen now shows Telegram app/call-aware guidance and a countdown-gated "Request another delivery method" button. SMS/call fallback language is shown only when Telegram returns a matching `next_type`; the app does not promise SMS by default.
- **Security/privacy:** Frontend responses never include `phoneCodeHash`. Incoming `phone_code_hash` and `session_string` remain stripped at the Edge proxy. Auth logs redact raw phone numbers to a stable hash and redact code/hash/session/password/token-like fields. No phone number, OTP, auth key, session string, API hash, password, or token was logged intentionally or documented.
- **CodeSettings audit:** Current GramJS `Api.CodeSettings` remains unchanged: `allowFlashcall=false`, `currentNumber=true`, `allowAppHash=true`. Telegram docs indicate `current_number` is ignored unless flash calls are enabled, and `delivery=app` is Telegram's server-selected delivery method. No evidence proved these flags caused missing app notifications, so they were not changed.
- **Listener/channel incident:** Production also showed GramJS update-loop timeout and listener/channel polling failures. This fix does not touch listener recovery, channel polling, trade execution, XAUUSD, range trading, broker errors, cache behavior, migrations, QR routing, or copier health semantics. Treat "connected but channels spin forever" as a separate follow-up incident unless staging proves it is tightly coupled to auth.
- **Tests / validation:** Added focused worker tests for resend wait, `auth.ResendCode` use, returned-hash replacement, verify using the latest hash, delivery/next_type/timeout capture, conservative timeout fallback, safe response shape, redacted logs, and deterministic Telegram auth error messages. Passed targeted worker tests, worker build, root build, and targeted ESLint for changed worker/shared component/edge files. Full repo ESLint still fails on unrelated existing lint debt in backoffice, dashboard/onboarding large pages, Supabase functions, and worker modules.
- **Files changed:** `worker/src/authService.ts`, `worker/src/authService.resend.test.ts`, `worker/src/authService.redaction.test.ts`, `worker/src/httpServer.ts`, `worker/src/httpServer.authErrors.test.ts`, `supabase/functions/telegram-auth/index.ts`, `src/lib/telegramAuthApi.ts`, `src/components/telegram/TelegramConnectFlow.tsx`, `src/pages/dashboard/CopierEnginePage.tsx`, `src/pages/onboarding/steps/TelegramLinkStep.tsx`, generated `dist/*`, generated `worker/dist/*`, and `docs/PROJECT_MEMORY-EMMA.md`.
- **Staging verification required:** Use staging with a permitted test account only. Verify initial delivery `app`, countdown, no Telegram call before timeout, `auth.resendCode` after timeout, delivery changing when Telegram offers a fallback, latest hash verification, 2FA/password flow, QR unaffected, and no sensitive auth values in logs/Sentry.

### 2026-08-13 - Telegram Phone-Code Auth Reliability Investigation

- **Context:** Investigated a Telegram phone-code login incident after a user disconnected and quickly reconnected the same Telegram number for onboarding recording. The requested scope was phone-code reliability only; no deploy, migration, commit, push, production Telegram login, OTP, phone number, session string, API hash, token, or password was used or exposed.
- **Current flow traced:** Product/onboarding UI calls `telegram-auth` Edge Function `send_code`, which proxies to the worker `/auth/send_code`. `AuthService.sendCode(...)` writes a `telegram_auth_pending` placeholder, asks `UserSessionManager.prepareForAuth(...)` to stop/release any live listener, connects a fresh GramJS client, invokes `Api.auth.SendCode`, stores the latest `phone_code_hash` in memory and `telegram_auth_pending`, then the UI advances to code entry only on a successful backend response. Code verification uses the stored/current hash server-side via `Api.auth.SignIn`; Telegram 2-step verification password is requested only after `SESSION_PASSWORD_NEEDED`; successful auth persists `telegram_sessions`, clears pending auth, and adopts the live auth client into the listener.
- **Disconnect/reconnect flow traced:** `disconnectTelegramSession(...)` deletes `telegram_auth_pending`, stops/releases the listener if present, deletes the `telegram_sessions` row, and writes copier health as not linked. It intentionally preserves configured Telegram channels. The remaining Telegram account claim is used for same-account ownership checks and is not a `sendCode` blocker for the same user/phone.
- **Root cause from code audit:** The code now calls Telegram `auth.SendCode` only after the live listener slot is released, and frontend code only shows the code-entry screen after the worker returns success. The incident cannot be proven from local code alone without staging/production auth logs, but the audited failure modes were: Telegram rate limiting or delivery to the Telegram app instead of SMS, not a missing frontend transition alone.
- **Fixes made:** Kept `phone_code_hash` server-side only by removing it from the `/auth/send_code` response. Added stable redaction for auth logs so phone numbers are hashed and code/hash/session/password/token values are fully redacted. Added duplicate in-flight `send_code` rejection to prevent repeated clicks from creating concurrent Telegram login-code requests. Broadened delivery mapping from GramJS `auth.SentCode.type.className` for app, SMS-family, and call-family delivery variants.
- **User-facing behavior:** The phone code screen can now receive a `delivery` value and show app/call-aware guidance instead of always implying SMS. Deterministic Telegram errors such as `FLOOD_WAIT`, `PHONE_NUMBER_FLOOD`, `SEND_CODE_UNAVAILABLE`, `PHONE_NUMBER_INVALID`, `PHONE_NUMBER_BANNED`, `API_ID_PUBLISHED_FLOOD`, `PHONE_CODE_EXPIRED`, and `PHONE_CODE_INVALID` are mapped to specific user-facing messages by the worker HTTP layer.
- **Recent recovery audit:** The malformed-RPC listener recovery path remains post-auth listener logic. Adopted auth-time clients are marked `canRecreateClient=false`, so malformed-RPC client recreation does not replace a freshly authenticated adopted client. The fix did not change trade execution, XAUUSD logic, range trading, broker errors, cache behavior, migrations, QR routing, or copier health semantics beyond existing auth state.
- **Tests / validation:** Focused worker tests passed for auth error mapping, auth recovery classification, redaction, and delivery mapping: `node --require ts-node/register --test src/httpServer.authErrors.test.ts src/authService.redaction.test.ts src/telegramAuthRecovery.test.ts`. Worker TypeScript build passed via `npm.cmd --prefix worker run build`; the build regenerated tracked `worker/dist` output.
- **Files changed:** `worker/src/authService.ts`, `worker/src/authService.redaction.test.ts`, `worker/src/httpServer.ts`, `worker/src/httpServer.authErrors.test.ts`, `src/lib/telegramAuthApi.ts`, `src/components/telegram/TelegramConnectFlow.tsx`, `src/pages/dashboard/CopierEnginePage.tsx`, `src/pages/onboarding/steps/TelegramLinkStep.tsx`, generated `worker/dist/*`, and `docs/PROJECT_MEMORY-EMMA.md`.
- **Remaining risks:** Live confirmation still requires staging auth logs or a controlled staging phone-code test, because local code inspection cannot prove whether Telegram delivered the specific user's code to app/SMS/call or rejected it with a flood wait. No external auth attempt was performed.

### 2026-08-12 - Deferred-Only Range Plan Registration Incident

- **Incident:** An Aug 11-style manual multi/range BUY allocation could produce a valid plan with zero immediate broker orders and one deferred range leg. The executor incorrectly treated that plan as empty because it checked only immediate orders before pending-leg registration. The user-facing result could look filtered/skipped even though the planner had produced a valid waiting/deferred leg.
- **Root cause:** `worker/src/tradeExecutor/entryPrepare.ts` had an empty-plan guard of `if (plan.orders.length === 0)`. That guard runs before `plan.virtualPendings` are copied, capped, materialized, and handed to the deferred trigger path. Therefore `orders=[]` and `virtualPendings=[...]` was rejected before `materializeVirtualPendingLegs(...)` could insert `range_pending_legs`.
- **Invariant:** A range plan is empty only when both immediate broker orders and deferred/waiting legs are empty. `orders.length === 0` alone means "no immediate broker order now"; it does not mean "no execution work".
- **Exact fix:** The guard now computes `const plannedVirtualPendings = plan.virtualPendings ?? []` and skips only when `plan.orders.length === 0 && plannedVirtualPendings.length === 0`. The later virtual list is sliced from the same planned array, preserving existing capping behavior.
- **Planner status:** The planner was correct. `planMultiManualOrders(...)` already had the combined `orders.length === 0 && virtualPendings.length === 0` fallback and can intentionally emit zero immediates plus deferred range legs for 100% range allocation.
- **Execution status after fix:** Deferred-only plans now proceed through normal `runRangeEntry(...)` materialization. `sendImmediateLegs(...)` already treats zero immediate legs as handled when virtual materialization or strict pending placement succeeded, so the downstream status contract did not need a parallel state.
- **Status/logging behavior:** A successfully registered deferred-only range plan is no longer logged through `logSendSkipped(..., 'filtered')` just because no immediate position opened. It uses the existing `virtual_pending_inserted` / pending terminology and returns `openedOrMerged: true` from the executor outcome after materialization.
- **Tests added:** Added planner regressions for 100% BUY range and equivalent SELL range producing `orders=[]` plus one virtual leg. Added `worker/src/tradeExecutor/deferredOnlyRangePlan.test.ts` covering BUY and SELL deferred-only registration without broker `OrderSend`, truly empty plan skip preservation, pending row shape (`status='pending'`, side, anchor, trigger), and trigger-side correctness for the existing virtual pending monitor machinery.
- **Guard audit:** Other immediate-empty guards were reviewed. `orderLegExecution.ts` already has the correct zero-immediate success behavior when `materializedVirtuals || strictBrokerPlaced`. `strictEntryPending.ts` requires capped immediates because it aggregates strict-entry broker pending orders, not range virtuals. The planner guard already checks both lists.
- **Downstream cleanup blocker found in final audit:** `worker/src/rangePendingBasketCleanup.ts` still treated `trades.status IN ('open', 'pending')` returning zero rows as proof that the basket was closed. That was valid for a basket that had already opened and then flattened, but invalid for deferred-only range plans because they intentionally have no immediate `trades` row while `range_pending_legs.status='pending'` is waiting for the monitor.
- **Cleanup root cause:** `reconcileBasketFlatFromBroker(...)` called `purgeRangePendingLegsIfBasketFlat(..., 'signal_closed')` when `openRows.length === 0`. The virtual pending monitor runs broker-flat reconciliation on its first tick, so a freshly inserted deferred-only row could be deleted before the price reached its trigger.
- **Active pending liveness invariant:** A signal/broker basket is active when it has open/pending trade rows, or active range pending rows, or another existing deferred execution state. For range pending rows the active statuses are exactly `pending`, `claimed`, and `broker_pending`. Terminal/non-liveness statuses include `fired`, `expired`, `cancelled`, and `failed`.
- **Exact cleanup fix:** `worker/src/rangePendingLegDelete.ts` now exports `ACTIVE_RANGE_PENDING_LEG_STATUSES` and checks active `range_pending_legs` scoped by `signal_id + broker_account_id` before flat-basket deletion. If there are no open/pending trades but an active range pending leg exists and no closed trade history exists for that same signal/account, cleanup returns without deleting it. If closed trade history exists, the prior flat-basket purge behavior is preserved so old active leftovers do not become immortal. `reconcileBasketFlatFromBroker(...)` now returns `null` when that liveness guard preserves the basket instead of reporting `signal_closed`.
- **Multi-account behavior:** The liveness and purge checks are scoped to the same `signal_id + broker_account_id` pair used by pending rows and dispatch claims. Cleanup for a closed broker account can delete that account's stale rows without touching another broker account's active deferred-only row for the same signal.
- **Deferred-only lifecycle after cleanup fix:** A BUY or SELL range plan with `orders=[]` and one active virtual pending now passes entry preparation, inserts a `range_pending_legs` row, survives the monitor's first reconciliation tick, remains eligible for the existing CAS claim path, and later fires through the existing virtual pending monitor when bid/ask crosses the trigger.
- **Additional tests:** Added cleanup regressions for zero trade rows plus active pending preserving the row, BUY and SELL deferred-only first-reconcile survival, zero trade rows plus no active pending purging stale state, terminal statuses not counting as active liveness, open trade plus pending preserving existing behavior, fired/materialized rows not keeping a dead basket alive, closed trade history allowing active leftovers to purge, multi-account isolation, two deferred-only accounts remaining isolated, and terminal-vs-active mixed account behavior.
- **Scope isolation:** No changes were made to range allocation percentages, lot splitting/rounding, pip math, XAUUSD conventions, SL/TP calculation, broker execution, or deferred trigger pricing.
- **Remaining risks:** This was validated with focused local tests and mocked execution context, not staging traffic. Full CI/staging should still verify live-fast pipeline summaries and UI copy for pending-only outcomes. Signal status may still read as executed/handled while a virtual pending is waiting; this is compatible with duplicate-dispatch prevention and monitor execution, but the wording remains UX debt. No production connection, migration, deploy, push, commit, or trade was performed.

### 2026-08-10 - User-Friendly Trade Failure Reasons

- **Branch:** `feat/user-friendly-trade-errors`.
- **Context:** Task 2 extended James/Martins' existing trade-error foundation instead of creating a parallel catalog. Existing pieces reused include `worker/src/brokerTradeError.ts`, broker metal symbol resolution in `worker/src/tradeExecutor/brokerSymbolCache.ts`, broker-error display in Copier Logs/Trade Detail, and the global assistant in `src/context/AssistantContext.tsx`.
- **Incidents covered:** Premium/VIP Stop Loss signals that include TPs but no executable SL, and GOLD/XAUUSD broker symbol mismatches that previously surfaced as generic broker failures or HTTP 500-style copy.
- **Structured reason contract:** Added a deterministic `TradeFailureReason` shape with `reasonCode`, `category`, `title`, `explanation`, `recommendedAction`, `retryable`, `userActionRequired`, and safe context. The first canonical reasons are `SIGNAL_MISSING_REQUIRED_SL` and `BROKER_SYMBOL_NOT_FOUND`; legacy `SYMBOL_UNSUPPORTED` maps to `BROKER_SYMBOL_NOT_FOUND`.
- **Premium/missing SL behavior:** The worker recognizes SL-withheld wording such as `SL premium`, `SL for premium members`, and `subscribe for SL`. It does not infer or invent an SL. The new missing-SL skip is emitted only when current manual execution cannot proceed safely: provider-withheld SL with no derivable configured fallback, or the existing explicit-stops-required path. SL-optional behavior remains unchanged.
- **Broker symbol behavior:** The existing metal resolver remains authoritative for `XAUUSD`, `GOLD`, suffix/prefix variants, `GOLD#`, `GOLD.pro`, and `XAUUSDm`. When matching still fails, the worker persists `BROKER_SYMBOL_NOT_FOUND` structured context instead of leaving only a vague broker error. Reconnect is not recommended for this case unless support determines symbol refresh/mapping is needed.
- **Persistence/API:** No migration was added. New structured context is stored in existing `trade_execution_logs.request_payload` JSON as `reason_code` and `trade_failure`, while preserving legacy `error_message` for old readers.
- **Frontend unification:** Added a central frontend display helper for structured trade failures and legacy fallbacks. Copier Log detail, Trade Detail, and the channel-worker log message path use it so explanations do not drift.
- **AI handoff:** Reuses the existing global assistant only. Failure sections can open the assistant with safe structured context and instructions that the reason code is authoritative, missing SL/TP must not be invented, and non-retryable outcomes must not be presented as safe retries.
- **Retryability/privacy:** Missing SL and broker symbol-not-found are non-retryable until user/config/support action. Broker timeout remains non-auto-retryable to preserve at-most-once safety. Safe AI/persistence context is allowlisted and excludes raw broker payloads, credentials, Telegram session data, OTPs, tokens, private account data, and raw signal text.
- **Files changed:** `worker/src/brokerTradeError.ts`, `worker/src/brokerTradeError.test.ts`, `worker/src/observability/businessEvents.ts`, `worker/src/tradeExecutor/entryPrepare.ts`, `worker/src/tradeExecutor/entryPrepareMissingSl.test.ts`, `worker/src/tradeExecutor/orderLegExecution.ts`, `worker/src/tradeExecutor/symbolMapping.test.ts`, `src/lib/tradeFailureDisplay.ts`, `src/lib/tradeFailureDisplay.test.ts`, `src/lib/copierLogDetail.ts`, `src/lib/copierLogDetail.test.ts`, `src/lib/channelWorkerLogMessage.ts`, `src/lib/channelWorkerLogMessage.test.ts`, `src/components/dashboard/CopierLogDetailModal.tsx`, `src/components/trades/TradeDetailModal.tsx`, `CHANGELOG.md`, and `docs/PROJECT_MEMORY-EMMA.md`.
- **Tests:** Added focused worker tests for withheld-SL phrase detection, no false positive for numeric SL or generic premium wording, SL-required vs SL-optional behavior, fallback-SL preservation, known broker reason mapping, legacy reason-code mapping, and metal resolver coverage. Added frontend tests for central display mapping, old log fallback, channel feed display, Copier Log detail extraction, and AI prompt privacy.
- **Remaining risks:** Localization is centralized but English-only for the new structured helper in this pass. Full end-to-end API behavior depends on existing worker/log polling boundaries; CI/staging should confirm no older UI path still renders only generic HTTP 500 for newly structured logs. No migration, deploy, push, or external-service check was performed.
- **Invariants:** Extend the existing broker formatter, do not replace it. Preserve the existing metal resolver and XAUUSD pip behavior. Do not place a trade to improve UX. Never invent missing SL. Keep SL-optional configurations unchanged. Reason code remains deterministic and authoritative. AI can explain the known reason but cannot override it. Ambiguous sends are not auto-retried. Old logs remain readable. Unexpected internal failures remain distinguishable from known business failures.

### 2026-08-10 - User-Friendly Trade Failure Blocker Follow-up

- **Branch:** `feat/user-friendly-trade-errors`.
- **Final audit blockers fixed:** The first pass incorrectly treated provider-withheld SL wording as enough to reject a trade whenever no fallback SL could be derived, even when the current manual configuration did not require SL. The retry path also still treated every failed entry signal as retryable without checking structured `trade_failure.retryable`.
- **SL-required rule:** The missing-SL failure now uses the existing authoritative entry-prep requirement: explicit stops are required only when `manual.add_new_trades_to_existing === false` and the parsed signal has no explicit stops. Premium/VIP wording only records `withheldByProvider`; it no longer makes SL globally mandatory. Existing predefined-pip or RR-derived fallback SL behavior still prevents the missing-SL failure.
- **Retryability enforcement:** Worker retry eligibility now consults the latest structured trade-execution log payload when present. `retryable: false` blocks retry before the legacy `status === failed` fallback; malformed/missing structured metadata preserves historical retry behavior. Frontend retry display uses the same structured retryability guard when available.
- **Tests:** Added focused coverage for premium-SL with required vs optional stops, multiple withheld-SL phrases, fallback SL preservation, false-positive premium/VIP wording, structured non-retryable reasons (`SIGNAL_MISSING_REQUIRED_SL`, `BROKER_SYMBOL_NOT_FOUND`, `INVALID_LOT`, `BROKER_TIMEOUT`), retryable temporary reasons, malformed metadata fallback, and frontend retry-button eligibility.
- **Resulting invariants:** SL is not globally mandatory. Premium wording identifies provider withholding only; it does not decide execution eligibility. Structured retryability is an additional safety guard and does not bypass entry-action, idempotency, claim, or ambiguous-send protections.

### 2026-08-14 - Telegram OTP App Delivery With No Resend Fallback

- **Production evidence:** Multiple independent users were unable to see Telegram login codes even though TScopier successfully reached Telegram. Railway logs showed `auth.SendCode` succeeding with `delivery=app`, `nextDelivery` absent, `timeoutSeconds` absent, and a returned `phone_code_hash`.
- **Impact pattern:** At least three users reproduced the same behavior. The shared infrastructure is the TScopier Telegram auth/listener stack and shared `TELEGRAM_API_ID` / `TELEGRAM_API_HASH` configuration; this patch does not prove Telegram API-app reputation as the root cause.
- **Root bug fixed:** `worker/src/authService.ts` fabricated a local resend availability time when Telegram omitted `timeout`. The UI then exposed “Request another delivery method” even though Telegram had not advertised `next_type` or a resend timeout.
- **Correct resend eligibility rule:** Resend is available only when Telegram returns both a `next_type` and a positive `timeout`. If Telegram returns `delivery=app` with no `next_type` and no `timeout`, the server returns `can_resend=false` and `resend_available_at=null`, and `resendCode` rejects locally with `NO_RESEND_AVAILABLE` without calling Telegram or replacing the pending `phone_code_hash`.
- **CodeSettings correction:** Server-side Node/GramJS `auth.SendCode` now uses conservative settings: `allowFlashcall=false`, `currentNumber=false`, `allowAppHash=false`, `allowMissedCall=false`, `allowFirebase=false`. Mobile-only token/app sandbox/logout-token behavior was not added.
- **QR fallback UX:** Shared Telegram connection UI now treats app/no-next/no-timeout as a no-fallback state: it says Telegram accepted the login request but did not offer another code delivery method, tells the user to check Telegram for a Telegram login message, hides resend/countdown, and offers the existing QR login flow as the primary alternative. It does not auto-switch to QR.
- **Safe telemetry:** Auth logs now include safe constructor/type metadata, delivery/next-delivery names, timeout presence/value, code length, `canResend`, correlation ID, and API error class/code. Logs still must not include raw phone numbers, OTPs, `phone_code_hash`, Telegram sessions, auth keys, API hashes, passwords, tokens, or logout tokens.
- **Disconnect semantics:** Manual Disconnect remains a local TScopier disconnect only: it deletes local session state and disconnects transport, but does not call Telegram `auth.LogOut`. Hard logout, future auth tokens, and Telegram authorization revocation are explicitly deferred for design review.
- **Tests added/updated:** Focused tests cover app/no-next/no-timeout returning `can_resend=false`, no fabricated 60-second timeout, local resend refusal without Telegram invocation or hash replacement, conservative `CodeSettings`, safe redaction for code length/API error code, valid next-type+timeout resend preservation, latest-hash verification preservation, http error copy for `NO_RESEND_AVAILABLE`, and shared UI hiding resend while showing QR fallback.
- **Staging acceptance criteria:** In staging, phone login with Telegram returning app/no-next/no-timeout should show no resend countdown/button, should show the QR CTA, should not mention SMS/call unless Telegram returned `next_delivery`, and should leave the pending hash authoritative until expiry or verification. Valid next-type+timeout attempts should still permit resend only after the returned timeout. QR login and 2FA should behave unchanged.
- **Validation note:** Run targeted worker auth tests, frontend Telegram UI tests, worker typecheck/build, root typecheck/build when dependencies permit, targeted ESLint, `git diff --check`, conflict-marker scan, and secret/privacy scan before staging. No deploy, commit, push, migration, production Telegram call, or real OTP request is part of this patch.

### 2026-08-10 - Telegram Malformed RPC Recovery Exhaustion

- **Branch:** `fix/telegram-recovery-user-errors`.
- **Production Sentry incident:** `telegram_recovery_exhausted` and `telegram_listener_failed` were observed together with reason code `GRAMJS_MALFORMED_RPC_RESULT` and message `Malformed Telegram RPC result recovery exhausted`.
- **Root cause:** The current malformed-RPC path in `worker/src/userListener.ts` counted GramJS malformed RPC results but did not perform real recovery until the counter exceeded the window limit. On exhaustion it treated the condition like AUTH_KEY recovery exhaustion by calling the auth-key invalidation callback, which could invalidate a still-valid Telegram session. Malformed RPC is a GramJS/MTProto transport or decoder failure signal, not proof that the persisted auth session is invalid.
- **Likely contributing conditions:** Existing incident docs point to GramJS BinaryReader malformed-body failures, update-loop transport state problems, reconnect races, and Telegram flood-wait/poll pressure. This change does not prove whether the originating malformed body was Telegram-side, network truncation, or GramJS state corruption; it fixes the unsafe local recovery behavior.
- **Recovery path after fix:** `client.onError` routes `GRAMJS_MALFORMED_RPC_RESULT`, `readUInt32LE`, and `Cannot read properties of undefined` to `noteMalformedRpcResult`. That path now marks recovery intent, joins the existing reconnect singleflight, disconnects, recreates the MTProto client when the listener owns the client instance, reuses the current saved session string, reconnects, requires a successful `updates.GetState` probe, then restores channel subscriptions and recent catch-up through the existing `forceReconnect` path.
- **Fix:** `worker/src/userListener.ts` now attaches the GramJS error handler through a reusable method so recreated clients keep the same handler. Malformed RPC calls `requestReconnect('malformed_rpc_result')` instead of only logging. `forceReconnect` snapshots the session before disconnect, recreates the client for malformed-RPC recovery when safe, probes before setting `isConnected=true`, resets malformed-RPC counters only after successful recovery, and never calls Telegram session invalidation for malformed RPC exhaustion.
- **Blocker follow-up:** Final safety audit found that owned-client recreation disconnected the old client before replacement but did not explicitly detach the current NewMessage/EditMessage handlers from the captured old client before `this.client` was reassigned. If GramJS kept dispatching after a failed or incomplete disconnect, duplicate signal processing was possible. The fix now captures `oldClient`, removes both current handlers from that explicit old client, clears handler references, then disconnects and only then creates/assigns the replacement client. Invariant: old Telegram client handlers must be detached before client replacement.
- **Files changed:** `worker/src/userListener.ts`, `worker/src/sessionManager.shutdown.test.ts`, `worker/src/copierHealth.ts`, `worker/src/copierHealth.test.ts`, `docs/copier-health-model.md`, `docs/sentry-business-observability.md`, and `docs/PROJECT_MEMORY-EMMA.md`.
- **Tests:** Focused tests cover malformed RPC triggering recovery, concurrent malformed errors joining the same reconnect, valid session preservation, successful recovery after reconnect/probe, probe-required recovery, recovery exhaustion without auth invalidation, old message/edit handler detachment before client replacement, disconnect failure leaving no old handlers registered, repeated malformed recoveries not accumulating handlers, shutdown during cleanup leaving old handlers detached, malformed exhaustion Sentry dedupe, auth-invalid behavior remaining distinct, copier-health recovery semantics, session shutdown/lease behavior, GramJS malformed-body patching, Telegram auth recovery helpers, business-event dedupe/redaction, Sentry redaction, targeted ESLint, and worker build.
- **Health behavior:** During malformed-RPC recovery the listener is reconnecting/degraded and is not falsely operational. Successful recovery requires a successful Telegram probe before returning to operational. Exhausted malformed-RPC recovery marks the listener failed/offline while keeping the Telegram account linked. Reconnect-required is reserved for proven auth/session invalidation.
- **Sentry behavior:** The duplicate incident pair was one underlying malformed-RPC recovery incident emitted twice. After this fix, malformed-RPC exhaustion emits `telegram_recovery_exhausted` with `GRAMJS_MALFORMED_RPC_RESULT`; the health transition remains persisted but does not also emit `telegram_listener_failed` for the same incident. Generic reconnect exhaustion still uses `telegram_listener_failed`.
- **Remaining risks:** The originating malformed RPC body cause remains unproven without production packet-level evidence. Recreating the client is skipped for adopted auth-time clients, where the listener does not own construction. Deferred retry behavior still depends on existing reconnect backoff settings. Full CI and staging soak are still required before production.
- **Rollback:** Revert this code change to return to prior behavior. Operationally, a rollback may reintroduce malformed-RPC session invalidation and duplicate Sentry issue emission; no database migration or cleanup is involved.
- **Invariants:** Do not clear or rotate persisted Telegram sessions for malformed RPC alone. Do not log session strings, phone numbers, auth keys, OTPs, raw Telegram messages, or tokens. Keep AUTH_KEY_UNREGISTERED/AUTH_KEY_DUPLICATED distinct from malformed RPC. Keep reconnect singleflight and worker ownership health CAS intact. Do not modify broker symbol handling, XAUUSD pip logic, trade execution, light config cache, migrations, deployment, or production service connections as part of this fix.

### 2026-08-07 - Staging Light Channel Config Cache

- **Branch:** `perf/light-channel-config-cache`.
- **Objective:** Implement the approved staging-first light configuration cache for the trade dispatch path. The goal is to reduce repeated Supabase reads for stable channel/account configuration without changing trade logic, XAUUSD pip logic, observability/copier-health semantics, durable claims, idempotency, broker readiness, or broker-send authority.
- **Original pre-broker DB-read path:** After a signal is accepted, `TradeExecutor.handleSignal` ensures the `signals` row exists, applies in-process duplicate protection, checks copier pause/subscription/broker eligibility, resolves channel metadata, and selects eligible brokers from the in-memory broker cache. For each broker, `sendOrder` first checks whether channel config is ready from cached broker state, then force-refreshes `broker_channel_trading_configs` through `fetchFreshBrokerForChannel(...)` before `withChannelTradingConfig(...)`, durable `claimSignalBrokerDispatch(...)`, and broker execution. Other live reads around this path include execution proof (`trades`, `signal_range_entry_waits`, `trade_execution_logs`), materialization/idempotency probes, range wait reads, and post-send signal finalization reads.
- **Read classification:** The repeated `broker_channel_trading_configs` row read is safe to cache briefly because it contains stable per-channel copier/manual/risk/layering/AI settings. `broker_accounts` are already cached in memory from startup + realtime. `telegram_channels` metadata is already cached by `channelMetaCache` and invalidated on channel updates. Copy-limit state already has its own short cache and was not moved into this feature. Durable claims, execution proof, idempotency/materialization checks, broker order state, current prices, open orders, broker connectivity/readiness, balance/equity/margin, kill switches, cancellation/reconciliation state, listener health ownership, and already-sent decisions remain explicitly excluded and live.
- **Implementation:** Added `worker/src/lightConfigCache.ts`, an in-memory per-worker cache around the pre-send `broker_channel_trading_configs` lookup. `LIGHT_CONFIG_CACHE_ENABLED=false` keeps the legacy database lookup path. When enabled, cache misses/expiry fetch the authoritative DB row, store a versioned entry, and apply the row to the broker before normal config resolution. Cached payloads are limited to the config row and recursively strip credential-like keys before storage. The final audit blockers were fixed before approval: stale in-flight fills are discarded after invalidation, invalidation uses structured exact identity instead of substring matching, and cache memory is bounded.
- **Entry shape / key:** Entries use `{ schemaVersion: 1, cachedAt, expiresAt, configVersionOrUpdatedAt, data }`, where `configVersionOrUpdatedAt` comes from `broker_channel_trading_configs.updated_at` when present. That field is informational only; it does not prevent stale writes. The stale-fill guard is the per-key generation/epoch. Keys are built from exact structured `{ userId, channelId, brokerAccountId }` identity with normalized channel ids and contain no secrets.
- **TTL / flags:** Defaults are `LIGHT_CONFIG_CACHE_ENABLED=false`, `LIGHT_CONFIG_CACHE_TTL_MS=5000`, and `LIGHT_CONFIG_CACHE_MAX_ENTRIES=1000`. Invalid TTL values fail safely by disabling cache use and reporting effective TTL `0`. Invalid max-entry values fall back to the safe default. Staging enablement requires setting `LIGHT_CONFIG_CACHE_ENABLED=true`, `LIGHT_CONFIG_CACHE_TTL_MS=5000`, and optionally keeping `LIGHT_CONFIG_CACHE_MAX_ENTRIES=1000`. Production must remain disabled until staging results are reviewed.
- **Invalidation:** `TradeExecutor.subscribeChannelTradingConfigs()` invalidates affected cache identities on `broker_channel_trading_configs` realtime before refreshing the existing broker cache. `INSERT` invalidates the new identity, `DELETE` invalidates the old identity, and `UPDATE` invalidates both old and new identities so broker/channel changes cannot leave the old cache key alive. When the broker owner is known in memory, invalidation is exact user+channel+broker; otherwise it falls back to a bounded structured broker+channel scan of known identities. No new Redis/pubsub subsystem was added; realtime invalidation plus the 5s TTL bounds staleness.
- **Stale-fill protection:** Each key has a generation counter. A DB fetch captures the current generation before it starts. Invalidation increments the generation and removes that key from the join map. If the old fetch later resolves, the caller may receive the DB result to preserve legacy semantics, but the result is not stored in cache and `light_config_cache_stale_fill_discarded` is counted. Newer generation fetches may populate normally, and failed fetches clear in-flight state.
- **Fallback behavior:** Valid hits use cached stable config. Misses, expired entries, malformed entries, invalid TTL, and disabled cache fetch DB. Cache subsystem errors are counted and bypassed; the wrapper falls back to the DB fetch. If the underlying authoritative DB lookup behaves as a no-row/no-update fallback, that existing behavior is preserved.
- **Safety boundaries:** The cache does not replace `ensureSignalRow`, `signalExecutionProven`, `manualDispatchAlreadyMaterialized`, `claimSignalBrokerDispatch`, broker session/readiness checks, broker symbol/quote/order calls, range wait state, cancellation state, reconciliation state, or post-send persistence. Cache failure must never stop trading/listening by itself, but required underlying DB failures preserve existing semantics.
- **Concurrency / memory:** The cache uses per-key singleflight. Concurrent identical user+channel+broker lookups await the same DB promise unless invalidation advances the generation; unrelated keys do not serialize. Completed and failed promises are removed. Entries are capped by `LIGHT_CONFIG_CACHE_MAX_ENTRIES` using deterministic recency/order eviction, and expired cold entries are pruned on get/set/invalidate without a high-frequency timer. At 50 users the default cap should be comfortably below limit for typical channel/account counts; at 500 users the hot set remains bounded; at 5,000 users hit rate may fall, but per-worker memory remains capped at 1000 entries unless explicitly tuned.
- **Metrics:** Added low-noise worker metrics for `light_config_cache_hit`, `light_config_cache_miss`, `light_config_cache_expired`, `light_config_cache_invalidated`, `light_config_cache_fallback_db`, `light_config_cache_malformed`, `light_config_cache_error`, `light_config_cache_singleflight_join`, `light_config_cache_stale_fill_discarded`, `light_config_cache_evicted`, `light_config_cache_pruned`, plus histogram-compatible lookup, cached-lookup, and DB-fallback duration metrics. Cache hit/miss/expiry does not emit Sentry issues.
- **Tests:** Added `worker/src/lightConfigCache.test.ts` covering disabled legacy behavior, first DB read, second hit within TTL, expiry/refetch, malformed entry refetch, cache failure fallback, DB failure semantics, exact invalidation, similar-id non-collision, update-style old/new invalidation, scoped fallback invalidation, unrelated entry retention, broker/risk setting changes not surviving beyond TTL, durable-claim/idempotency/broker-state/kill-switch/cancellation exclusions, duplicate-dispatch non-involvement, singleflight join, failed/successful inflight clearing, invalidation during inflight, cache-disable during inflight, multiple invalidations during inflight, max-entry eviction, cold expired pruning, 5,000 synthetic keys bounded to max, key separation, TTL/max-entry env diagnostics, config round-trip equivalence, metrics, and no secret storage.
- **Docs / env:** Added `docs/light-config-cache.md`, updated `docs/worker-deployment.md`, and added `LIGHT_CONFIG_CACHE_ENABLED=false`, `LIGHT_CONFIG_CACHE_TTL_MS=5000`, and `LIGHT_CONFIG_CACHE_MAX_ENTRIES=1000` guidance to `worker/.env.example`.
- **Staging rollout:** Deploy code with the flag off first. Enable only on staging trade workers with `LIGHT_CONFIG_CACHE_ENABLED=true`, `LIGHT_CONFIG_CACHE_TTL_MS=5000`, and `LIGHT_CONFIG_CACHE_MAX_ENTRIES=1000`. Measure reduced repeated config reads, lower config lookup p50/p95, correct realtime invalidation after settings changes, no stale broker/risk/layering settings beyond TTL, no stale-fill discard spikes after settings updates, no duplicate trades, unchanged durable claims/idempotency/kill-switch behavior, and no cache-caused broker failures.
- **Production-readiness pass:** The same artifact is intended to run in staging and production. Production deploys keep `LIGHT_CONFIG_CACHE_ENABLED=false`, preserving the legacy DB refresh path with no migration or persisted cache state. After staging approval, production enablement is configuration-only with the reviewed TTL/max-entry values. Rollback is also configuration-only: set `LIGHT_CONFIG_CACHE_ENABLED=false`; any in-memory entries are ignored by new dispatches and no DB/cache cleanup is required.
- **Production safety boundaries:** The cache scope remains only `broker_channel_trading_configs` and only the audited execution-relevant subset (`copier_mode`, `manual_settings`, `ai_settings`, `copy_limit_state`, `updated_at`). Durable claims, idempotency, duplicate execution proof, broker order state, prices, open orders, broker connectivity, balance/equity/margin, kill switches, cancellation/reconciliation state, listener health ownership, and anything proving an order was already sent remain live and uncached. A code comment at the integration point documents this boundary for future engineers.
- **Multi-worker behavior:** The cache is per-process volatile memory. Worker A and worker B do not share entries; realtime should invalidate each subscribed worker; missed invalidation is bounded by TTL; worker restart starts empty; shard/ownership movement does not copy cache state; no correctness assumption requires one global worker.
- **Settings-change guarantee:** Normal frontend, Edge Function, admin/backoffice, service-role, direct DB update, delete/recreate, broker disconnect/delete, and channel removal/unlink paths are expected to invalidate through `broker_channel_trading_configs` realtime when delivered. Do not claim universal instant invalidation: if realtime is missed or not delivered for a path, stale config is bounded by `LIGHT_CONFIG_CACHE_TTL_MS` (default 5s).
- **Production metrics / alerts:** Operational review should use hit rate `hit / (hit + miss)`, fallback rate `fallback_db / total lookups`, and error rate `cache_error / total lookups`. Warnings include cache error rate above 1% for 5 minutes, unexpectedly high fallback after warm-up, continuous stale-fill-discard spikes, hit rate near 0 for stable traffic, or sustained realtime/invalidation gaps. Disable cache immediately for any confirmed stale broker/account config use, duplicate trade, wrong risk config, execution outcome change, or cache exception reaching the broker hot path.
- **Staging acceptance gate:** Before production enablement, staging must show zero cache-attributable duplicate trades, zero stale broker selections, zero stale risk-setting incidents beyond TTL, zero claim/idempotency changes, zero kill-switch or broker-readiness bypass, zero cache-caused trade failures, fewer config reads, meaningful hit rate, improved config lookup p50, no p95/pre-broker regression, working invalidation/fallback/disable rollback, and memory within the configured bound. Use meaningful traffic: one internal account/channel first, then small cohort, then normal staging load.
- **Failure modes:** Disabled -> DB path; hit -> cached stable config; miss/expired/malformed/cache exception -> DB path; realtime missed -> stale bounded by TTL; invalidation during DB fetch -> stale completion discarded; cache full -> oldest/least-recent entry evicted; worker restart -> empty cache and DB warm-up; metrics failure -> execution unaffected; DB failure -> legacy DB failure semantics.
- **Security / privacy:** Cache keys contain no secrets and metrics do not label user/channel/broker ids. Cached values are in-process volatile memory only and must not contain broker passwords, API keys, access tokens, Telegram sessions, Supabase secrets, raw Telegram messages, raw broker payloads, balance, equity, or margin.
- **Rollback:** Set `LIGHT_CONFIG_CACHE_ENABLED=false` and restart/redeploy the trade worker. No migration, data cleanup, or code rollback is required.
- **Known risks:** Cross-worker invalidation depends on Supabase Realtime delivery; the 5s TTL is the safety bound if realtime is missed. The cache deliberately does not solve distributed caching or long-lived configuration reuse. Production enablement requires staging evidence.
- **Do not break these invariants:** (1) Cache off equals legacy behavior. (2) Only `broker_channel_trading_configs` is cached. (3) Durable claims are never cached. (4) Idempotency is never cached. (5) Broker order state is never cached. (6) Prices are never cached. (7) Kill switch remains live. (8) Broker readiness remains live. (9) Cache failure falls back safely. (10) DB failures preserve legacy semantics. (11) Stale entries are never served after expiry. (12) Realtime invalidation matches structured cache identity. (13) Invalidation during an in-flight DB fetch cannot resurrect stale config. (14) Same-key concurrent misses singleflight safely. (15) Failed fetch cannot permanently poison a key. (16) Cache cannot cause duplicate trade execution. (17) Cached and uncached data have equivalent execution semantics for current config fields. (18) No secrets are cached. (19) Memory cannot grow unbounded indefinitely. (20) Disabling the flag is a safe rollback.

### 2026-08-07 - Deferred Business Events + Accurate Copier Health

- **Branch:** `feat/deferred-events-copier-health`.
- **Relevant commits:** `064e3635` (`feat(observability): add deferred events and accurate copier health`) and `8c5e5124` (`fix(observability): complete deferred failure coverage`). No later local commits were present on this branch at the time this memory entry was written.
- **Context / objective:** This branch fixes two observability gaps before promotion from `dev`. First, several user-impacting failures happened after the main trade path had already reported success and were visible only in Railway/console logs. Examples include virtual pending materialization failure, broker-pending materialization failure, post-fill SL/TP failure, basket TP sync failure, management cleanup failure, and broker-success/database-persistence mismatch. Second, the dashboard previously inferred Live/Online mostly from lease freshness and Telegram session-row existence; neither proves that the Telegram listener is connected, MTProto is healthy, or signals are being processed.
- **Root cause - deferred events:** Fire-and-forget post-fill/background work often logged final failures only to console. These failures were not consistently represented as structured Sentry business events, so support had to search Railway logs to understand partial outcomes after a broker order succeeded.
- **Root cause - copier health:** `worker_session_leases` freshness represented listener ownership, not live MTProto/listener health. `telegram_sessions` row existence represented Telegram linkage, not active signal listening. Stale health could survive worker death, and the final audit also identified that old workers needed ownership-aware CAS hardening so they could not overwrite health now owned by a newer worker.
- **Deferred business-event implementation:** Added centralized structured business-event helpers for final deferred failures only. Successful follow-ups do not emit events; per-retry attempts do not emit events; capture is fire-and-forget; no Sentry capture affects trade outcome. A broker-success path remains successful even if later follow-up work partially fails, so the system records a partial/manual-review outcome instead of retroactively marking the successful broker order as failed.
- **Deferred event names to know:** `layering_materialization_failed`, `post_fill_follow_up_failed`, `stop_loss_update_failed`, `take_profit_update_failed`, `basket_tp_sync_failed`, `trade_management_cleanup_failed`, `broker_success_persistence_failed`, and `deferred_trade_follow_up_failed`.
- **Deferred reason codes to know:** `VIRTUAL_MATERIALIZATION_FAILED`, `BROKER_PENDING_MATERIALIZATION_FAILED`, `POST_FILL_SL_UPDATE_FAILED`, `POST_FILL_TP_UPDATE_FAILED`, `BASKET_TP_SYNC_FINAL_FAILURE`, `MGMT_CLOSE_CLEANUP_FAILED`, `BROKER_SUCCESS_DB_FAILURE`, `BROKER_PENDING_FILL_DB_FAILURE`, and `BASKET_RECONCILE_ENQUEUE_FAILED`.
- **Copier health implementation:** Added `copier_listener_health` persistence and a server-authoritative state model for Telegram account state, signal listener connectivity, copier engine status, and worker ownership. Worker health now records actual listener/MTProto connectivity, ownership identity, lease acquisition epoch, recent successful probe time, shutdown/recovery flags, freshness thresholds, and user-safe reason codes. Frontend status reads this health model instead of treating a fresh lease or session row as proof of operational readiness.
- **Health state model:** Telegram account states are `linked`, `not_linked`, `invalid`, and `reconnect_required`. Signal listener states are `connected`, `reconnecting`, `disconnected`, `failed`, and `unknown`. Copier engine states are `operational`, `degraded`, `offline`, and `stopped`. Worker ownership states are `owned`, `lease_expiring`, `unowned`, and `stale`.
- **Critical health rule:** Operational must not be inferred from a lease alone. Operational requires a valid linked session, active ownership, connected listener, `mtproto_connected=true`, a recent successful probe, a recent health row, no shutdown state, and no exhausted recovery.
- **Stale row safety:** Frontend and worker fail closed when `updated_at` is stale, `last_successful_probe_at` is stale, timestamps are missing/malformed, or ownership is stale. A stale row must never show Operational indefinitely.
- **Stale worker safety:** Health writes use ownership-aware CAS through `upsert_copier_listener_health(...)`. Stale owner writes return `stale_ownership`. Those failed health writes do not stop listener/trading. Old workers cannot overwrite health owned by a newer worker. Health persistence is operational metadata, not execution authority.
- **Grace period / write frequency:** `COPIER_HEALTH_OFFLINE_GRACE_MS=60000` by default. Short reconnects show degraded/reconnecting; reconnect/disconnect beyond grace becomes offline; exhausted recovery can fail faster; shutdown should not create a false critical outage. Health writes happen on meaningful state transition, and unchanged state writes are bounded to about 30-second intervals. There are no per-second DB writes, and health write failure does not affect trading/listening.
- **Frontend UX:** Dashboard status is separated into Telegram account, Signal listener, Copier engine, and Last healthy time. Normal users should not see MTProto, lease, shard, worker ownership, or heartbeat jargon. A Telegram session row can mean Telegram is linked, but it cannot make the signal listener Connected or copier engine Operational.
- **Sentry health behavior:** Only meaningful health issue events should be emitted, primarily `copier_engine_offline` and `telegram_listener_failed`. Do not emit issues for every health probe, lease renewal, brief reconnect, successful reconnect, or normal shutdown. Repeated failures use cooldown/grouping.
- **Migration:** Added `supabase/migrations/20260806120000_copier_listener_health.sql`. It is additive, enables RLS, lets authenticated users read only their own safe health state, prevents normal clients from writing worker health, uses service-role worker writes, adds ownership-aware CAS RPC, and stores no secrets/session strings. This memory update did not remotely apply the migration.
- **Important files/modules:** `worker/src/copierHealth.ts`, `worker/src/copierHealth.test.ts`, `worker/src/userListener.ts`, `worker/src/sessionManager.ts`, `worker/src/observability/deferredBusinessEvents.ts`, `worker/src/observability/businessEvents.test.ts`, `worker/src/postFillFollowUp.ts`, `worker/src/rangeBasketTpSync.ts`, `worker/src/rangeBrokerPendingMonitor.ts`, `worker/src/virtualPendingMonitor.ts`, `worker/src/tradeExecutor/TradeExecutor.ts`, `worker/src/tradeExecutor/orderLegExecution.ts`, `worker/src/tradeExecutor/virtualPendingMaterialize.ts`, `worker/src/tradeExecutor/materializeBrokerRangePendingLegs.ts`, `src/lib/copierHealthStatus.ts`, `src/lib/copierHealthStatus.test.ts`, `src/components/dashboard/CopierStatusCard.tsx`, `supabase/migrations/20260806120000_copier_listener_health.sql`, `docs/copier-health-model.md`, `docs/sentry-business-observability.md`, `docs/worker-deployment.md`, and `CHANGELOG.md`.
- **Validation recorded for this branch:** Focused validation should cover copier health tests, virtual pending monitor tests, frontend copier health tests, business/Sentry tests, Telegram auth/recovery regressions, session lease/shutdown tests, queue tests, management/regression batches, root typecheck, worker typecheck, targeted ESLint, root build, worker build, and `git diff --check`. Do not claim the full worker suite passed unless CI or a local run actually completed. This memory-only update does not change runtime code and only re-checks documentation diff cleanliness.
- **Known issues / out of scope:** XAUUSD pip convention alignment is unrelated to this feature and is CTO-owned separately. Light configuration caching is approved for staging evaluation but is not part of this branch. No remote migration, deploy, push, or external-service check has been performed as part of this memory update. CI should still validate the complete integration state before merge.
- **Do not break these invariants:** (1) Fresh lease alone must never mean Operational. (2) Telegram session row alone must never mean listener Connected. (3) Operational requires a recent successful listener probe. (4) Old workers must not overwrite newer health ownership. (5) Health persistence failures must never stop trading/listening. (6) Sentry capture must never block the trade hot path. (7) One final failure should emit one Sentry issue, not duplicate issues. (8) Successful broker trades must not be retroactively marked failed because a later follow-up failed. (9) Do not store Telegram sessions, broker secrets, tokens, raw messages, or full broker payloads in copier health/Sentry context. (10) Channel-specific failure should not mark the entire copier Offline unless listener capability is genuinely unavailable.
- **Approved follow-up:** Light short-lived configuration caching for staging should be developed separately. It must not cache durable claims, idempotency, broker order state, prices, margin, broker connectivity, kill switches, or cancellation state. Proposed direction only: very short TTL, versioned configuration, invalidation on settings changes, database fallback, metrics, and staging-first validation.
- **Changelog review:** `CHANGELOG.md` already contains concise release-note entries for deferred business-event capture, copier health state, ownership-aware persistence, stale freshness, and duplicate Sentry suppression. No changelog rewrite was needed for this memory-only update.
- **Follow-up:** Apply the additive health migration through the normal promotion process, deploy worker/frontend through staging, verify health rows and dashboard wording with real listener transitions, and run CI/full integration checks before merging.
